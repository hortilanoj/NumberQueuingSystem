<Button x:Class="NumberQueuingSystem.FRONTDESK.Views.ucButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="94.594" Width="395.932" Margin="3" Command="{Binding Click_Command}" Padding="5" IsEnabled="{Binding CanPress}">
    <Button.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF6262CB" Offset="0"/>
            <GradientStop Color="#FF9090C5" Offset="1"/>
        </LinearGradientBrush>
    </Button.Background>
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Trans_Name}" HorizontalAlignment="Center" FontSize="24"/>
            <StackPanel>
                <TextBlock Text="{Binding Trans_Desc}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Button>
